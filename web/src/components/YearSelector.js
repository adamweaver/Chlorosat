/* [AI] Purpose: Let users pick which year to view.
 *      Does:    STUB. Planned: buttons or a slider built from
 *               region.layers[methodId].years (manifest.json); calls onChange(year).
 *      Context: TODO(CS-022, Kevin). Years can differ per method, so rebuild the list when
 *               the method changes. Must work by keyboard and on phones.
 *               Planned props: { years, value, onChange }
 *      Written: 2026-09-22 · Claude Opus 5.5 · requested by Adam Weaver */
export default function YearSelector() {
  return null;
}
