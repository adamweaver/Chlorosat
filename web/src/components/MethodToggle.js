/* [AI] Purpose: Let users switch between the two ways we detect vegetation.
 *      Does:    STUB. Planned: one button per entry in manifest.methods (label = method.name,
 *               short help text = method.description); calls onChange(methodId).
 *               Starts on manifest.defaultMethod. Hide or disable methods with no years
 *               for the current region.
 *      Context: TODO(CS-022, Kevin). The methods can disagree (see docs/research/visible-detection.md),
 *               so always make it obvious which one is showing. Keyboard + phone friendly.
 *               Planned props: { methods, value, onChange }
 *      Written: 2026-09-22 · Claude Opus 5.5 · requested by Adam Weaver */
export default function MethodToggle() {
  return null;
}
