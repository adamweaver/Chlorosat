/* [AI] Purpose: Show simple numbers about the selected method + year (or change between years).
 *      Does:    STUB. Planned: % area per class (labels from the method's classes), average
 *               value, data source + dates + resolution, all in plain words. In compare mode,
 *               show change stats instead.
 *      Context: TODO(CS-022, Kevin; compare mode CS-023, Adam).
 *               Data shape: <year>.json / change JSON in docs/ARCHITECTURE.md#data-contract.
 *               Always say which method the numbers come from (methods can disagree).
 *               Planned props: { method, stats, change }
 *      Written: 2026-09-22 · Claude Opus 5.5 · requested by Adam Weaver */
export default function StatsPanel() {
  return null;
}
