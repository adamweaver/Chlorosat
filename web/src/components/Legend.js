/* [AI] Purpose: Explain what each map color means, in plain words.
 *      Does:    STUB. Planned: title = the active method's name, then one row per class
 *               (color swatch + label). Transparent classes (e.g. "About the same") get an
 *               outlined empty swatch.
 *      Context: TODO(CS-022, Kevin). Classes come from manifest.json (method.classes, or
 *               method.changeClasses in compare mode). Never hard-code colors, so the legend
 *               always matches the PNGs.
 *               Planned props: { method, classes }
 *      Written: 2026-09-22 · Claude Opus 5.5 · requested by Adam Weaver */
export default function Legend() {
  return null;
}
