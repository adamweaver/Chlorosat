# --- OS / editor junk ---
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# --- Secrets (never commit) ---
.env
.env.*
*.pem
*.key
# ssh-keyscan output: contains the server address
*known_hosts*
# SSH private keys (the .pub halves are harmless, but keep them out too)
id_rsa*
id_ecdsa*
id_ed25519*
chlorosat-ci*

# --- Python / pipeline ---
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
# Raw + intermediate satellite data: large, re-downloadable
pipeline/data/*
!pipeline/data/.gitkeep

# --- Web (Next.js) ---
node_modules/
web/.next/
# Built site: CI builds this and deploys it; never commit
web/out/
next-env.d.ts
npm-debug.log*
